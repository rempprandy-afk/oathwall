{
  "name": "merrymen-worker-marker",
  "private": true,
  "type": "module",
  "description": "Marker only — the worker ships as part of the root merrymen package; deps live in the root package.json."
}
