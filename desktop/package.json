{
  "name": "merrymen-desktop",
  "version": "0.1.7",
  "private": true,
  "description": "merrymen — the one-click desktop app. Bundles Node, boots the agent worker + dashboard, opens it in a native window. No terminal, no npm.",
  "author": "merrymen",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "stage": "node stage-bundle.mjs",
    "unstage": "node stage-bundle.mjs --restore",
    "dist": "npm run stage && electron-builder && npm run unstage",
    "dist:win": "npm run stage && electron-builder --win && npm run unstage",
    "dist:mac": "npm run stage && electron-builder --mac && npm run unstage",
    "dist:linux": "npm run stage && electron-builder --linux && npm run unstage",
    "check": "node --check main.js && node --check stage-bundle.mjs"
  },
  "dependencies": {
    "merrymen": "file:.."
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8"
  }
}
