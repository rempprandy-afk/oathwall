{
  "name": "oathwall-core-marker",
  "private": true,
  "type": "module",
  "description": "Marker only — core ships inside the root oathwall package and is resolved via tsconfig paths (@oathwall/core)."
}
