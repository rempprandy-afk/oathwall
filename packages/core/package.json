{
  "name": "merrymen-core-marker",
  "private": true,
  "type": "module",
  "description": "Marker only — core ships inside the root merrymen package and is resolved via tsconfig paths (@merrymen/core)."
}
